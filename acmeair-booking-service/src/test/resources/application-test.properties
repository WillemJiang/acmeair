#Just disable the service discovery when running the unit test
spring.cloud.discovery.enabled=false
logging.level.root=INFO

customer.service.address=http://localhost:8082
spring.main.web-environment=true
